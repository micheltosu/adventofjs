import { getInputForDAy } from '../../util/InputFetcher';

async function main() {
    const input = await (await getInputForDay('2022','07')).split('\n');


    const nodes = [];
    for (const line: string of input) {
        switch (line.charAt)
        console.log(line);
    }

}

main();
    